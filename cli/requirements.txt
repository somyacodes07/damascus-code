# Damascus CLI — Python Dependencies
# =====================================
# Usage (recommended — virtual env):
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -e .   ← installs `damascus` command

# CLI framework
typer[all]==0.15.1
click==8.1.7

# TUI framework
textual==0.74.0

# Rich console output
rich==13.8.1

# HTTP client to call backend
httpx==0.27.2
